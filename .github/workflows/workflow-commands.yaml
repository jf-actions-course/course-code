name: Workflow Commands
on: [push]

jobs:
    testing-wf-commands:
        runs-on: ubuntu-latest
        steps:
            - name: Setting an error message
              run: echo "::error::Missing semicolon"
            - name: Setting an error message with params
              run: echo "::error title:Error Title,file:app.js,line:2,endLine=3,col=5,endColumn=7::Missing Semicolon"
            - name: Setting an debug message with params
              run: echo "::debug title:Debug Title,file:app.js,line:2,endLine=3,col=5,endColumn=7::Debug Message"
            - name: Setting an warning message with params
              run: echo "::warning title:Warning Title,file:app.js,line:2,endLine=3,col=5,endColumn=7::Warning Message"
            - name: Setting an information error message with params
              run: echo "::info title:Info Title,file:app.js,line:2,endLine=3,col=5,endColumn=7::Info Message"
            - name: Group of logs
              run: |
                echo "::group::My Group Title"
                echo "Inside group"
                echo "::endgroup::"
            - name: Masking a value
              run: "::add-mask::my-secret-value"
            - name: Echo a secret
              run: echo "my-secret-value"
